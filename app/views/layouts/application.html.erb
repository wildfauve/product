<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Products" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="icon-bar six-up">
      <a class="item">
        <i class="fi-home"></i>
        <label>Home</label>
      </a>
      <%= link_to(sales_products_path, class: "item") do %>
        <i class="fi-thumbnails"></i>
        <label>Sales Products</label>
      <% end %>
      <%= link_to(tasks_path, class: "item") do %>
        <i class="fi-magnifying-glass"></i>
        <label>tasks</label>
      <% end %>      
      <%= link_to(algorithms_path, class: "item") do %>
        <i class="fi-widget"></i>
        <label>Algorithms</label>
      <% end %>            
    </div>    


    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>